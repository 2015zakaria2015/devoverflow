name: Deploy Next.js App

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'  # Ensure this matches your preferred Node.js version

      - name: Install dependencies
        run: |
          npm install --legacy-peer-deps  # Uses legacy-peer-deps to avoid conflicts

      - name: Build the project
        run: npm run build

      - name: Deploy to server
        run: |
          ssh -o StrictHostKeyChecking=no -i "$PRIVATE_KEY" ubuntu@$HOST << 'EOF'
            cd /var/www/html/devoverflow
            git pull origin main
            npm install --legacy-peer-deps
            npm run build
            pm2 restart nextjs-app  # Ensure pm2 is properly set up on your server
          EOF
